window.onload=start;

function start()
{
    var horasIngresadas;
    var precio;

    horasIngresadas=parseInt(prompt("Ingrese cantidad de horas"));
    if(horasIngresadas>=1&&horasIngresadas<=2)
    {
        precio=horasIngresadas*40;
    }
    else if(horasIngresadas==)
}